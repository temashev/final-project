[pytest]
asyncio_mode = auto
asyncio_default_test_loop_scope = session
markers =
    login: test register and login pipeline
    register: test registration

[tool.coverage.run]
omit = ["app/api/ui_*.py"]